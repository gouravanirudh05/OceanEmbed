# Core
numpy>=1.24,<2
scipy>=1.10
pandas>=2.0
pyyaml>=6.0
tqdm>=4.65

# Deep learning (CPU wheels are fine; install the CUDA build for GPU training)
torch>=2.2

# Geospatial / NetCDF I/O
xarray>=2023.1
netCDF4>=1.6
pyshp>=2.3            # rasterising the Natural Earth coastline

# Baselines
scikit-learn>=1.3

# Figures and dashboard
matplotlib>=3.7
plotly>=5.15
streamlit>=1.30

# Tests
pytest>=7.4

# --- optional, only needed for the real-data path (see oceanembed/data/cmems.py)
# copernicusmarine>=1.3   # OSTIA SST, SMOS/SMAP SSS, DUACS SLA, GLORYS target
# earthaccess>=0.9        # OSCAR currents, CCMP / ASCAT winds
